# Bin2Chars
Bin2Chars is a C++ library that does conversions from integer and floating types to an array of characters (string); it implements optmized algorithms leveraging SIMD instructions for x86_64 and other tricks for ARM64; making conversions much faster than libstdc++ and libc++ implementations.
